-- ============================================================================
-- V4: 미사용 미러 테이블 제거 (이슈 #80)
-- ----------------------------------------------------------------------------
-- game_sessions / user_scenario_progress 의 실데이터는 game-engine(MongoDB)이 소유.
-- 도메인 삭제로 PostgreSQL 측 미러 테이블의 사용처가 사라져 제거한다.
-- 두 테이블을 참조하는 인바운드 FK 없음 → 단순 DROP. IF EXISTS 로 재실행 안전.
-- ============================================================================
DROP TABLE IF EXISTS game_sessions;
DROP TABLE IF EXISTS user_scenario_progress;
